spring:
  grpc:
    server:
      port: 9091
    client:
      channels:
        state-store:
          address: localhost:9092
  datasource:
    url: jdbc:h2:mem:db
    username: sa
    password: password
  h2:
    console:
      enabled: true
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8081

management:
  server:
    port: 8081

logging:
  level:
    org.janus.policystore: DEBUG