spring:
  grpc:
    client:
      channels:
        policy-store:
          address: localhost:9091
        state-store:
          address: localhost:9092
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 1s

logging:
  level:
    org.janus.decider: DEBUG