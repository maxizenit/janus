spring:
  grpc:
    client:
      channels:
        policy-store:
          address: localhost:9091
        state-store:
          address: localhost:9092
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 1s

management:
  server:
    port: 8083

logging:
  level:
    org.janus.decider: DEBUG