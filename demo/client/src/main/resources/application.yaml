demo:
  server:
    url: http://localhost:8090
    connect-timeout: 1s
    read-timeout: 3s

spring:
  application:
    name: demo-client
  grpc:
    client:
      channels:
        sidecar:
          address: localhost:9094

server:
  port: 8091

management:
  server:
    port: 8091
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    tags:
      application: ${spring.application.name}
    distribution:
      percentiles-histogram:
        http.client.requests: true
        http.server.requests: true

logging:
  level:
    org.janus: DEBUG

janus:
  sdk:
    enabled: true
    sidecar-channel: sidecar
    refresh-interval: 5s
    stale-strategy: last-value

resilience4j:
  enabled: false
  circuitbreaker:
    instances:
      recommendations:
        sliding-window-type: TIME_BASED
        sliding-window-size: 10
        minimum-number-of-calls: 20
        failure-rate-threshold: 50
        wait-duration-in-open-state: 5s
        permitted-number-of-calls-in-half-open-state: 5
        register-health-indicator: true
        # Slow-call detection. These are the Resilience4j defaults (rate 100%,
        # duration 60s) and so leave the "naive" r4j config unchanged. The
        # r4j-slow config overrides them via env (relaxed binding) to make the
        # circuit breaker latency-aware: SLOWCALLRATETHRESHOLD=50,
        # SLOWCALLDURATIONTHRESHOLD=1000ms. Declaring them here ensures the
        # property is known to Spring so the env override binds reliably.
        slow-call-rate-threshold: 100
        slow-call-duration-threshold: 60s