apiVersion: v1
kind: Service
metadata:
  name: demo-server
  labels:
    app.kubernetes.io/name: demo-server
    app.kubernetes.io/component: demo
    app.kubernetes.io/part-of: janus
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: demo-server
  ports:
    - name: http
      port: 8090
      targetPort: 8090
