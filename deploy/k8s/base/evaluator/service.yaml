apiVersion: v1
kind: Service
metadata:
  name: evaluator
  labels:
    app.kubernetes.io/name: evaluator
    app.kubernetes.io/component: platform
    app.kubernetes.io/part-of: janus
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: evaluator
  ports:
    - name: management
      port: 8081
      targetPort: 8081
